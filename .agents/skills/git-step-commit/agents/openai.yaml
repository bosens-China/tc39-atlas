interface:
  display_name: "Git Step Commit"
  short_description: "Configure preferences, commit changes, and safely push"
  default_prompt: "Use $git-step-commit to configure commit preferences or organize, commit, and safely push my Git changes."
