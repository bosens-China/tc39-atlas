# ES2022 能力速览

> 本文件由 TC39 Atlas Dataset 自动生成，是随仓库更新的快照。最新提案集合、Stage、状态和版本请读取 [llms.txt](https://bosens-china.github.io/tc39-atlas/llms.txt)；需要详细内容时打开条目中的 Atlas 提案 Markdown。

共 12 项已进入 ES2022 的提案。

- [.at()](https://bosens-china.github.io/tc39-atlas/proposals/proposal-relative-indexing-method.md) — Stage 4 · 已完成
  该提案为 Array、String 和 TypedArray 添加一个 `.at()` 方法，支持类似于 Python 的负索引。该方法接受一个整数；负索引从末尾开始计数，越界访问返回 `undefined`。它最初提议命名为 `.item()`，但因库的鸭子类型判断而被发现与 Web 不兼容，因此更名为 `.at()`。

- [Accessible Object.prototype.hasOwnProperty（可访问的 Object.prototype.hasOwnProperty）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-accessible-object-hasownproperty.md) — Stage 4 · 已完成
  该提案引入了 Object.hasOwn(object, property) 方法，以提供更便捷的方式来检查自有属性，避免了 Object.prototype 方法不可用或被覆盖的问题。它简化了常见的写法，如 Object.prototype.hasOwnProperty.call(object, prop)。

- [Class Fields (Private instance methods and accessors, Class Public Instance Fields & Private Instance Fields, Static class fields and private static methods)（类字段（私有实例方法与访问器、公共实例字段与私有实例字段、静态类字段与私有静态方法））](https://bosens-china.github.io/tc39-atlas/proposals/proposal-private-methods.md) — Stage 4 · 已完成
  该提案解决了 JavaScript 类中此前无法使用的私有实例方法、访问器和静态方法的需求。它已被整合到更广泛的类字段提案中，该提案还涵盖了公共和私有实例字段以及静态字段。该仓库现为历史存档，权威信息来源已移至合并后的提案仓库。

- [Class Static Block（类静态初始化块）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-class-static-block.md) — Stage 4 · 已完成
  该提案在类体中引入了静态初始化块，允许在类定义求值期间执行任意语句，从而在类内部实现 try/catch 逻辑和多字段初始化，并拥有对私有状态的特权访问。

- [Ergonomic brand checks for Private Fields（私有字段的人体工程学品牌检查）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-private-fields-in-in.md) — Stage 4 · 已完成
  该提案解决了在不抛出异常的情况下检查对象是否具有私有字段的问题，目前需要使用笨拙的 `try`/`catch` 模式。主要解决方案引入了 `in` 运算符来测试私有字段的存在性，例如 `#brand in obj`。

- [Error Cause（错误原因）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-error-cause.md) — Stage 4 · 已完成
  该提案为 `Error()` 构造函数添加了一个 `cause` 选项，允许错误链带有原始原因的上下文信息。它解决了错误包装缺乏标准约定、难以诊断嵌套失败的问题。js 等主要环境中广泛实现。

- [Extend TimeZoneName Option Proposal（扩展 TimeZoneName 选项提案）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-intl-extend-timezonename.md) — Stage 4 · 已完成
  该提案扩展了 Intl.DateTimeFormat 中的 timeZoneName 选项，以支持四种新格式：shortOffset、longOffset、shortGeneric 和 longGeneric，超越了现有的 short 和 long。它旨在根据 UTS35 提供更灵活的时区显示选项。

- [Intl Enumeration API（Intl 枚举 API）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-intl-enumeration.md) — Stage 4 · 已完成
  该提案新增了 Intl 方法 `Intl.supportedValuesOf(key)`，返回 calendar、collation、currency、numberingSystem、timeZone 和 unit 等选项所支持的取值数组。它支持对 ECMA402 支持情况进行功能检测，使 Web 应用能在需要时高效导入 polyfill。

- [Intl.DisplayNames v2（Intl.DisplayNames 第二版）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-intl-displaynames-v2.md) — Stage 4 · 已完成
  该提案扩展了 Intl.DisplayNames，以支持更多类型，包括日历名称和日期时间字段名称，并添加了 languageDisplay 选项来控制语言名称以方言还是标准形式显示。

- [Intl.Segmenter: Unicode Segmentation in JavaScript（Intl.Segmenter：JavaScript 中的 Unicode 分段）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-intl-segmenter.md) — Stage 4 · 已完成
  该提案引入了 Intl.Segmenter API 用于 Unicode 文本分段，解决了根据 locale 特定规则将字符串拆分为字素、单词或句子的需求。它提供了可迭代和随机访问的接口来获取带有元数据的分段。

- [RegExp Match Indices（RegExp 匹配索引）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-regexp-match-indices.md) — Stage 4 · 已完成
  该提案在 `RegExp.prototype.exec` 等相关方法返回的数组结果上增加 `indices` 属性，提供每个捕获组相对于输入字符串的起始和结束索引。它引入了 `d` 标志，仅在需要时启用此功能以优化性能。

- [Top-level await（顶层 await）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-top-level-await.md) — Stage 4 · 已完成
  该提案在 ECMAScript 模块中引入了顶层 `await`，允许模块在顶层直接等待异步操作。它解决了使用 IIAFE 或导出 Promise 来处理模块初始化依赖时的竞争条件和复杂性问题。
