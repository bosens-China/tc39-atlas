# ES2019 能力速览

> 本文件由 TC39 Atlas Dataset 自动生成，是随仓库更新的快照。最新提案集合、Stage、状态和版本请读取 [llms.txt](https://bosens-china.github.io/tc39-atlas/llms.txt)；需要详细内容时打开条目中的 Atlas 提案 Markdown。

共 8 项已进入 ES2019 的提案。

- [Array.prototype.{flat,flatMap}](https://bosens-china.github.io/tc39-atlas/proposals/proposal-flatMap.md) — Stage 4 · 已完成
  该提案旨在为 ECMAScript 添加两个数组方法：`flat()` 用于扁平化嵌套数组，`flatMap()` 用于映射元素并将结果扁平化一层。

- [Function.prototype.toString revision（Function.prototype.toString 修订）](https://bosens-china.github.io/tc39-atlas/proposals/function-prototype-tostring-revision.md) — Stage 4 · 已完成
  该提案修订了Function.prototype.toString，旨在跨不同实现提供更一致和有用的输出。它旨在明确内置函数和用户自定义函数的toString的确切行为。

- [JSON superset（JSON 超集）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-json-superset.md) — Stage 4 · 已完成
  该提案通过允许字符串字面量中未转义的 U+2028 和 U+2029，将 ECMAScript 语法扩展为 JSON 的完整超集，解决了与 JSON.parse 的不一致。该变更向后兼容，并消除了这些罕见字符的 SyntaxError。

- [Object.fromEntries](https://bosens-china.github.io/tc39-atlas/proposals/proposal-object-from-entries.md) — Stage 4 · 已完成
  该提案引入了 Object.fromEntries，这是一个静态方法，将可迭代的键值对转换为对象，作为 Object.entries 的逆操作。它解决了轻松将 Map 或键值对数组等集合转换为普通对象而无需手动 reduce 循环的需求。

- [Optional catch binding（可选 catch 绑定）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-optional-catch-binding.md) — Stage 4 · 已完成
  此提案解决了在故意忽略错误的场景中，常见的使用未使用 catch 绑定（例如 `catch (unused)`）的做法，这种做法可能暗示编程错误。它对 ECMAScript 引入了语法变更，允许省略 catch 绑定及其圆括号，从而支持 `catch { . }` 这样的语法。

- [String.prototype.{trimStart,trimEnd}（String.prototype.{trimStart, trimEnd}）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-string-left-right-trim.md) — Stage 4 · 已完成
  该提案旨在标准化 String.prototype.trimStart 和 trimEnd 方法，它们分别移除字符串开头或结尾的空白字符。它还引入了 trimLeft 和 trimRight 作为别名以确保 Web 兼容性，并匹配 padStart/padEnd 的命名模式。

- [Symbol.prototype.description](https://bosens-china.github.io/tc39-atlas/proposals/proposal-Symbol-description.md) — Stage 4 · 已完成
  该提案为 Symbol.prototype 添加了 `description` 属性，提供符号的字符串描述。它解决了在不依赖隐式字符串转换（被禁止）的情况下访问符号描述的问题。

- [Well-formed JSON.stringify](https://bosens-china.github.io/tc39-atlas/proposals/proposal-well-formed-stringify.md) — Stage 4 · 已完成
  该提案解决了 JSON.stringify 可能返回包含未配对代理码点的格式不正确的 Unicode 字符串的问题，这些字符串不是有效的 UTF-8 或 UTF-16。它提议用 JSON 转义序列（例如 \\udf06）来表示此类未配对的代理，同时保留有效代理对的序列化。
