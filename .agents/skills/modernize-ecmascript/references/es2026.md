# ES2026 能力速览

> 本文件由 TC39 Atlas Dataset 自动生成，是随仓库更新的快照。最新提案集合、Stage、状态和版本请读取 [llms.txt](https://bosens-china.github.io/tc39-atlas/llms.txt)；需要详细内容时打开条目中的 Atlas 提案 Markdown。

共 9 项已进入 ES2026 的提案。

- [Array.fromAsync](https://bosens-china.github.io/tc39-atlas/proposals/proposal-array-from-async.md) — Stage 4 · 已完成
  该提案添加了一个 Array.fromAsync 静态方法，用于将异步可迭代对象、同步可迭代对象和类数组对象异步转换为数组，类似于 Array.from 但面向异步迭代。它支持可选的映射函数和 thisArg，匹配 for await 的语义，包括惰性迭代和错误处理，并且是一个泛型工厂方法。

- [Error.isError（Error.isError 提案）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-is-error.md) — Stage 4 · 已完成
  该提案引入静态方法 `Error.isError`，用于可靠地检测一个值是否为原生 Error 实例，解决了跨全局对象下 `instanceof` 不可靠以及通过 `Symbol.toStringTag` 伪造 `Object#toString` 的问题。

- [Intl Era and MonthCode Proposal（Intl 纪元和 MonthCode 提案）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-intl-era-monthcode.md) — Stage 4 · 已完成
  该提案为 Temporal 集成中非 ISO 日历的 era、eraYear 和 monthCode 指定了详细行为，旨在填补 ECMA-402 规范中的空白。它定义了抽象操作、有效的 era 代码、月份代码以及针对 gregory、roc、buddhist、japanese 等日历的错误条件。

- [Intl Locale Info（Intl 区域设置信息）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-intl-locale-info.md) — Stage 4 · 已完成
  该提案为 `Intl.Locale` 添加了方法，以公开区域设置特定信息，包括周数据（第一天、周末）、文本方向以及日历、排序规则、小时周期、编号系统和时区的默认值。

- [Iterator Sequencing（迭代器序列化）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-iterator-sequencing.md) — Stage 4 · 已完成
  该提案添加了一个静态的 `Iterator.concat` 方法，用于将多个可迭代对象按顺序组合成一个迭代器。它解决了手动使用生成器或辅助库进行拼接在人体工程学上的不足。

- [JSON.parse source text access（JSON.parse 源文本访问）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-json-parse-with-source.md) — Stage 4 · 已完成
  本提案解决了 ECMAScript 值与 JSON 文本之间转换有损的问题，特别是对于数字和像 BigInt 这样的特殊对象。它扩展了 JSON.parse，向 reviver 函数传递源文本和上下文，并添加了 JSON.rawJSON 以实现无损失真。

- [Math.sumPrecise](https://bosens-china.github.io/tc39-atlas/proposals/proposal-math-sum.md) — Stage 4 · 已完成
  该提案引入了 Math.sumPrecise，这是一个接受可迭代对象并返回其数值总和的方法，精度高于朴素求和。它解决了求和列表的常见操作，并提高了浮点数的准确性。

- [Uint8Array to/from Base64（Uint8Array 与 Base64 之间的转换）](https://bosens-china.github.io/tc39-atlas/proposals/proposal-arraybuffer-base64.md) — Stage 4 · 已完成
  该提案添加了 `Uint8Array.prototype.toBase64()` 和 `toHex()`，以及静态方法 `fromBase64()` 和 `fromHex()`，用于在二进制数据和 base64 或十六进制字符串之间进行转换。它还引入了 `setFromBase64`/`setFromHex` 用于写入现有数组，并提供选项来处理字母表、最后块处理和填充。

- [Upsert](https://bosens-china.github.io/tc39-atlas/proposals/proposal-upsert.md) — Stage 4 · 已完成
  该提案解决了在更新或插入之前检查 Map 或 WeakMap 中键是否存在的常见问题，这需要多次查找。它引入了 getOrInsert 和 getOrInsertComputed 方法，这些方法返回现有值或在一次调用中插入默认值或计算值。
